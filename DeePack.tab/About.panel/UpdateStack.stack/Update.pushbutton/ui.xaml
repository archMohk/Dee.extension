<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update DeePack"
        Height="400" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <StackPanel Margin="24" HorizontalAlignment="Center">
        <Image x:Name="logo_img" Width="72" Height="72" Margin="0,0,0,12"/>
        <TextBlock Text="Update DeePack" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,12"/>
        <TextBlock TextWrapping="Wrap" TextAlignment="Center" Width="360" Margin="0,0,0,16"
                   Text="Pulls the latest version from GitHub (if this install was set up with 'pyrevit extend'), then reloads pyRevit to pick up the changes. A renamed ribbon TAB specifically still needs a full Revit restart to show up."/>
        <Button x:Name="website_b" Content="www.archmkd.com" Click="website_click"
                HorizontalAlignment="Center" Padding="14,6" Margin="0,0,0,16" Cursor="Hand"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="update_b" Content="Update Now" Click="update_click" Width="120" Height="30" Margin="0,0,10,0" FontWeight="Bold"/>
            <Button x:Name="cancel_b" Content="Cancel" Click="cancel_click" Width="100" Height="30"/>
        </StackPanel>
    </StackPanel>
</Window>
